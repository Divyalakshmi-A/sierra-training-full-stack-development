{
  "xsappname": "library-management",
  "tenant-mode": "dedicated",
  "scopes": [
    {
      "name": "$XSAPPNAME.Admin",
      "description": "Full administrative access to the library system"
    },
    {
      "name": "$XSAPPNAME.Member",
      "description": "Read books, categories, and issue records"
    }
  ],
  "role-templates": [
    {
      "name": "Admin",
      "description": "Library administrator with full CRUD access",
      "scope-references": ["$XSAPPNAME.Admin"]
    },
    {
      "name": "Member",
      "description": "Library member with read-only catalog access",
      "scope-references": ["$XSAPPNAME.Member"]
    }
  ],
  "oauth2-configuration": {
    "redirect-uris": [
      "https://*.cfapps.*.hana.ondemand.com/**",
      "https://*.applicationcloud.hana.ondemand.com/**",
      "http://localhost:5000/**"
    ]
  }
}
